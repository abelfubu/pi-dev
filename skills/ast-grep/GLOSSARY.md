# Glossary

**Structural**
A search that depends on code syntax shape — function calls, class definitions, imports, argument positions, control flow — rather than text. The leading word for the `ast-grep` skill.
